FROM postgres:16

COPY init-user.sh /root/init-user.sh
COPY drizzle/0000_silent_gertrude_yorkes.sql /root/khi.sql

RUN chmod +x /root/init-user.sh && /root/init-user.sh

# no need for CMD here because the image will auto start the postgres process